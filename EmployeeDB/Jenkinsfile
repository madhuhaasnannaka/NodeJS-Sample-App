pipeline {
    agent any
    stages {
        stage('Build') {
 
            steps {
                sh 'sudo docker build ./EmployeeDB -t madhuhaas/nodejs:v0'
                sh 'sudo docker push madhuhaas/nodejs:v0'
            }
        }
    }
}
